This is the simplest possible implementation of Express NodeJS server with multiple endpoints.

> you can start server by `ts-node server.ts` command or can build with `tsc` and run by `node server.js`