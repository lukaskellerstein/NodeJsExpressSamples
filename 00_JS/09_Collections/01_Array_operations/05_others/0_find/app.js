// ------------------------------
// FIND

// searching:
// Array (primitives) -> primitive (number, string, boolean)
// Array (objects) -> object

// INPUTS
// searching condition function = (value, i, arr){}

// ---------------
// is used for:
// 1) search first occurence

// ------------------------------

var array = [1, 2, 3, 4, 5];

var result = array.find((val, i, arr) => {
  //return true/false
});
