// ------------------------------
// FILTER

// transformation
// Array -> Array

// INPUTS
// filter condition function - (value, i, arr){}

// ---------------
// is used for:
// 1) filtering data

// ------------------------------

var array = [1, 2, 3, 4, 5];

var result = array.filter((value, i, arr) => {
  //return true/false
});
