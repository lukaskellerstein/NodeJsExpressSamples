// ------------------------------
// MAP

// transformation
// Array -> Array

// INPUTS
// needs modify function - (value, i, arr){}

// ---------------
// is used for:
// 1) modifying data
// 2) trnasformation data
// 3) selecting ex. part of object

// ------------------------------

var array = [1, 2, 3, 4, 5];

var result = array.map((value, i, arr) => {
  // INPUTS
  // value = current value
  // i = index of current value
  // arr = the whole original array
  //
  //
  // SOME LOGIC
  //
  //
  // RETURN
  // return value/object the new array
});
